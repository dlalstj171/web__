{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run with Local Server",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5500/main.html",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
